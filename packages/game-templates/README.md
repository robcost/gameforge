# game-templates

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build game-templates` to build the library.

## Running unit tests

Run `nx test game-templates` to execute the unit tests via [Vitest](https://vitest.dev/).
